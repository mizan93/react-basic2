import React from 'react'

const Carone = (props) => {
    const{ brand, color}=props
    return (
        <div>
            <h1>Carone brand {brand} color {color}</h1>
        </div>
    )
}

export default Carone
